[pytest]
trio_mode = true
# Disable the upstream pytest-textual-snapshot plugin because we use a vendored
# version in tests/pytest_textual_snapshot.py that fixes syrupy 5.0 compatibility
addopts = -v -p no:textual-snapshot
